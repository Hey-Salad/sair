import { StreamsView } from "@/components/streams/streams-view"

export const metadata = {
  title: "Streams — HeySalad",
  description: "Live camera feeds with AI object detection",
}

export default function StreamsPage() {
  return <StreamsView />
}
